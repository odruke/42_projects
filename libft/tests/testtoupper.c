#include "libft.h"

int	main()
{
	int imin;
	int imay;
	char cmin;
	char cmay;

	imin = 'a';
	imay = 'A';
	cmin ='a';
	cmay = 'A';

	printf("todo mayusculas int %i, %i, char %c, %c", ft_toupper(imin),ft_toupper(imay),ft_toupper(cmin),ft_toupper(cmay));
}
