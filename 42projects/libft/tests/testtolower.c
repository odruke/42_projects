#include "libft.h"

int	main()
{
	int imin;
	int imay;
	char cmin;
	char cmay;

	imin = 'a';
	imay = 'A';
	cmin ='a';
	cmay = 'A';

	printf("todo minusculas int %i, %i, char %c, %c", ft_tolower(imin),ft_tolower(imay),ft_tolower(cmin),ft_tolower(cmay));
}
